{
    "private": true,
    "workspaces": [
        "z-sharp",
        "utils"
    ],
    "scripts": {
        "build": "yarn workspace utils run build && yarn workspace z-sharp run build",
        "package": "yarn workspace z-sharp run package"
    }
}
